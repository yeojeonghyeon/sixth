CREATE TABLE USERS(
  USER_ID  VARCHAR2(10),
  USER_NAME  VARCHAR2(50),
  PASSWORD  VARCHAR2(50),
  ENABLED NUMBER(1)
  );
ALTER TABLE USERS ADD CONSTRAINT PK_USERS PRIMARY KEY(USER_ID);

CREATE TABLE USERS_ROLES (
  user_role_id VARCHAR2(10),
  user_id VARCHAR2(10),
  authority VARCHAR2(45)
);
ALTER TABLE USERS_ROLES ADD CONSTRAINT PK_USERS_ROLES PRIMARY KEY(user_role_id);
ALTER TABLE USERS_ROLES ADD CONSTRAINT FK_USERS_ROLES_USER_ID FOREIGN KEY(USER_ID) REFERENCES USERS(USER_ID); 


INSERT INTO USERS
VALUES('0000000002'
,'PET'
,'1234'
,1);

INSERT INTO USERS_ROLES
VALUES(
'1000000002',
'0000000002',
'ROLE_ADMIN'
);

COMMIT;